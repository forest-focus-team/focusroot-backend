spring:
  datasource:
    url: jdbc:mysql://localhost:3306/focusroot_db?useSSL=false&serverTimezone=UTC
    username: root
    password: password_cua_ban # Hãy đổi thành mật khẩu MySQL local của bạn
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
